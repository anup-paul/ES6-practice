person = {name:'Anup', age:21, phone:'01676029196', address:'jigatola'};
const pname = person.name;
const page = person.age;
console.log(pname, page);

const {phone, address} = person;
console.log(phone,address);